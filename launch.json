{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "WDIO Test",
            "program": "${workspaceFolder}/node_modules/.bin/wdio",
            "args": ["run", "${workspaceFolder}/config/wdio.conf.ts"],
            "console": "integratedTerminal",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}